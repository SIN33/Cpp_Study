スタックアロケータ―概要

allocate(リクエスト個数)
{
	スタックポインター += (リクエスト個数 * ブロックサイズ)
	return スタックポインター
}

deaoolcate(リクエスト個数)
{
	スタックポインター += (リクエスト個数 * ブロックサイズ)
	return スタックポインター
}

上記に加え、
・リクエストサイズに応じたフリーリスト(キャッシュプール)を作成
・割り当てたスタックポインターからキャッシュプ―ルを割り出す
